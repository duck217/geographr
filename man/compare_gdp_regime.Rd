% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_gdp_regime.R
\name{compare_gdp_regime}
\alias{compare_gdp_regime}
\title{Compare GDP per capita across regime types using ANOVA}
\usage{
compare_gdp_regime(data, year = NULL)
}
\arguments{
\item{data}{A data frame containing at least \code{regime_category} and
\code{gdp_per_capita_nominal} columns (e.g. the output of \code{\link[=load_data]{load_data()}}).}

\item{year}{Optional single numeric year to restrict the analysis to.
If \code{NULL} (default), all years are used.}
}
\value{
A list with two elements:
\describe{
\item{anova}{A tidy data frame with the ANOVA table (term, df, sumsq,
meansq, statistic, p.value).}
\item{tukey}{A data frame of Tukey HSD pairwise comparisons with columns
contrast, estimate, conf.low, conf.high, and adj.p.value.}
}
}
\description{
Performs a one-way ANOVA to test whether mean GDP per capita differs
significantly across regime categories, then returns pairwise Tukey HSD
post-hoc comparisons so the caller can see which pairs of regime types
drive the differences.
}
\examples{
\dontrun{
dat <- load_data()
results <- compare_gdp_regime(dat)
results$anova
results$tukey

results_2015 <- compare_gdp_regime(dat, year = 2015)
}

}
